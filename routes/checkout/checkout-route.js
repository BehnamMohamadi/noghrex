import {Router} from 'express';import {authenticate} from '../../middlewares/auth.js';import {validate} from '../../middlewares/validate.js';import * as c from '../../controllers/checkout/checkout-controller.js';import {createCheckoutSchema} from '../../validations/checkout/checkout-validation.js';const r=Router();r.use(authenticate);r.post('/',validate(createCheckoutSchema),c.create);r.get('/:id',c.get);export default r;
