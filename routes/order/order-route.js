import {Router} from 'express';import {authenticate} from '../../middlewares/auth.js';import {validate} from '../../middlewares/validate.js';import * as c from '../../controllers/order/order-controller.js';import {createOrderSchema} from '../../validations/order/order-validation.js';const r=Router();r.use(authenticate);r.get('/',c.list);r.post('/',validate(createOrderSchema),c.create);r.get('/:id',c.get);r.post('/:id/pay/wallet',c.payWallet);r.post('/:id/cancel',c.cancel);export default r;
