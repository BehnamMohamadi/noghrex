import {Router} from 'express';import {authenticate,authorize} from '../../middlewares/auth.js';import {validate} from '../../middlewares/validate.js';import {create,update} from '../../controllers/shipping/shipping-controller.js';import {shippingMethodSchema} from '../../validations/shipping/shipping-validation.js';const r=Router();r.use(authenticate,authorize('admin'));r.post('/',validate(shippingMethodSchema),create);r.patch('/:id',validate(shippingMethodSchema),update);export default r;
