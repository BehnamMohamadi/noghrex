import {Router} from 'express';import {authenticate} from '../../middlewares/auth.js';import {validate} from '../../middlewares/validate.js';import * as c from '../../controllers/address/address-controller.js';import {createAddressSchema,updateAddressSchema} from '../../validations/address/address-validation.js';const r=Router();r.use(authenticate);r.get('/',c.list);r.post('/',validate(createAddressSchema),c.create);r.patch('/:id',validate(updateAddressSchema),c.update);r.delete('/:id',c.remove);export default r;
